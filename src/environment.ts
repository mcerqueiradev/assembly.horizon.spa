// Default (development) environment.
// Replaced by environment.prod.ts on production builds (see angular.json fileReplacements).
export const environment = {
  production: false,
  apiUrl: 'https://localhost:7220',
};
